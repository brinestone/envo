{
  "name": "envo",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "pnpm nx run-many -t=dev --parallel",
    "build": "pnpm nx run-many -t=build",
    "migrate.local": "pnpm run --filter backend migrate.local",
    "drop.local": "pnpm run --filter backend drop.local",
    "release": "standard-version"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  },
  "devDependencies": {
    "nx": "21.4.1",
    "standard-version": "^9.5.0",
    "tsup": "^8.5.0"
  }
}