{
  "name": "envo",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "predev": "rm -rf apps/console/.angular",
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --parallel build",
    "migrate.local": "pnpm run --filter backend migrate.local",
    "drop.local": "pnpm run --filter backend drop.local",
    "release": "standard-version"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  },
  "devDependencies": {
    "standard-version": "^9.5.0",
    "tsup": "^8.5.0"
  }
}
