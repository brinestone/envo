{
  "name": "envo",
  "private": true,
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "migrate.local": "pnpm run --filter backend migrate.local"
  }
}
